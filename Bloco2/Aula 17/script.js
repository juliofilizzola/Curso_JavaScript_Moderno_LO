// sobre funções 

function name(params) {
  console.log(params);
}

name('jose');

// arrow function

// const name = params => console.log(params);

// name('antonio');

const some = (a, b) => a + b * a;

console.log(some(5,6));
